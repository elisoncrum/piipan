openapi: 3.0.0
info:
  title: "State Matching API"
  version: 1.0.0
  description: "API for detecting matching PII records within an individual state."
tags:
  - name: "Match"
servers:
  - url: "/v1/"
paths:
  /query:
    post:
      tags:
        - "Match"
      summary: "Search for matching PII records"
      description: "Queries the state database for any PII records that are an exact match to the full name, date of birth, and social security number in the request body's `query` property."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                query:
                  $ref: '#/components/requestBodies/matchQuery'
      responses:
        '200':
          description: "Matching PII records, if any exist"
          content:
            application/json:
              schema:
                type: object
                properties:
                  matches:
                    items:
                      $ref: '#/components/schemas/PIIRecord'
        '400':
          description: "Bad request. Missing one of the required properties in the request body."
          
components:
  schemas:
    PIIRecord:
      $ref: '../models/pii-record.yaml'
  requestBodies:
    matchQuery:
      $ref: '../models/match-query.yaml'
